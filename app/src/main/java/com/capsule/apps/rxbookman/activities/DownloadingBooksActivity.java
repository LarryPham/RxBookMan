package com.capsule.apps.rxbookman.activities;

import android.os.Bundle;
import android.support.annotation.Nullable;

import com.capsule.apps.rxbookman.utils.LogUtils;

public class DownloadingBooksActivity extends BaseActivity {

    private static final String TAG = LogUtils.makeLogTag(DownloadingBooksActivity.class);

    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
    }

    @Override
    protected void handleBackStackChanged() {

    }

    @Override
    protected void invalidate() {

    }

    @Override
    protected void invalidate(Object... params) {

    }
}
